Vendored [quint-connect](https://github.com/informalsystems/quint-connect)
0.1.1 uses Quint's built-in TypeScript backend with one worker. Model tests
therefore need only the `quint` CLI and do not download a separate evaluator.
