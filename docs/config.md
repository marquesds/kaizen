# Config

## Crate Roles

| Crate | Role |
|---|---|
| kaizen | Binary entry point, orchestration |

<!-- Add crate roles as dependencies grow -->

## Environment Variables

| Var | Required | Default | Purpose |
|---|---|---|---|
| `RUST_LOG` | no | `info` | Log level filter |
| `PORT` | no | `8080` | HTTP listen port (if applicable) |

<!-- Add vars as they are introduced -->

## Configuration Files

<!-- Document any config file formats (TOML, YAML, etc.) -->
